@model ICollection<DemoApplication.Models.Campaigns.ChooseProductViewModel>

<h4>Step 1: Choose from our Popular Packages</h4>


<table class="table table-striped">
    <tr>
        <th>
            Name
        </th>
        <th>
            
        </th>
    </tr>
    
    @if (Model != null)
    {
        foreach (var product in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(p => product.ProductName)
                </td>
                <td>
                    
                </td>
            </tr>
        }
    }

    
</table>
